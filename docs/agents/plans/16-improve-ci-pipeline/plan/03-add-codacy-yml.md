# Add minimal .codacy.yml
Add a minimal `.codacy.yml` at the repo root, with no exclusions yet, mirroring the convention used in the Majora project. This gives a place to add static-analysis exclude paths later without needing a follow-up issue just to introduce the file.

## Files to Change
- `.codacy.yml` (new) — minimal Codacy configuration, no exclude paths yet.
