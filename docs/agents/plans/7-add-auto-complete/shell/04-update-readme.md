# Update README

Register the new script and folder per the project convention ("New scripts registered in table in README.md").

## Files to Change

- `README.md` — add `completions/` to the `## Structure` tree (with a short comment like `# Bash completion script for tingle`); add an `install` row to the `## Scripts` table (Language: Shell); mention `tingle install` in the `## Commands` section alongside the existing `tingle help`/`--help` description, noting it wires up `PATH` and bash completion via `~/.bashrc`.
