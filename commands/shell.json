{
  "install": {
    "path": "shell/install.sh",
    "short_help": "Install tingle onto PATH and enable bash completion.",
    "long_help": "Adds tingle's bin/ folder to PATH and installs bash completion for tingle, both idempotently, by appending the necessary lines to ~/.bashrc if they are not already present.\n\nUsage:\n    tingle install\n\nRunning it more than once is safe: existing PATH and completion entries in ~/.bashrc are detected and left untouched."
  }
}
