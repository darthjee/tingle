{
  "check_file_size": {
    "path": "python/check_file_size/main.py",
    "short_help": "Token efficiency triage: file size analysis.",
    "long_help": "Analyzes source files and lists them by size (line count), classifying them by configurable thresholds. Useful for identifying token consumption bottlenecks before feeding a repository to an AI.\n\nUsage:\n    ./check_file_size.py <path> [options]\n\nExamples:\n    ./check_file_size.py ./src\n    ./check_file_size.py ./src --warn 300 --error 500 --critical 1000\n    ./check_file_size.py ./src --top 20\n    ./check_file_size.py ./src --exclude node_modules,dist,build\n    ./check_file_size.py ./src --ext .py --ext .js"
  },
  "kube": {
    "path": "python/kube/main.py",
    "short_help": "Kubernetes (EKS) subcommand with a scoped alias layer.",
    "long_help": "Kubernetes (EKS) helper built around short, scoped aliases for contexts, namespaces, and pods, backed by ~/.tingle/kube/config.json. Reads and validates that config (schema: version, aws_profile, pod_id_pattern, shell, contexts, namespaces, pods), applying defaults (aws_profile: default, pod_id_pattern: ^[a-z0-9]{10}$, shell: bash) when fields are absent, and degrades gracefully to pass-through mode when the config is missing or invalid.\n\nUsage:\n    tingle kube switch <context_alias>\n    tingle kube list namespace\n    tingle kube list pods\n    tingle kube shell <namespace_alias> <pod_alias>\n    tingle kube configure context\n    tingle kube configure namespace\n    tingle kube configure pod\n\nExamples:\n    tingle kube switch prod\n    tingle kube list namespace\n    tingle kube list pods\n    tingle kube shell web api\n    tingle kube configure context"
  }
}
